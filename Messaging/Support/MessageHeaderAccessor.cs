using System;

namespace Cloudmark.Messaging.Support {
	/// A base class for classes providing simple type getters and setters as well as behaviour around 
	/// specific categories of headers.  Supports creating new headers, modifying existing headers (when still mutable) 
	/// or copying nad modifying existing headers.  
	public class MessageHeaderAccessor {
		private final MutableMessageHeaders headers;
		
		public MessageHeaderAccessor() {
			this.
		}
	}
}